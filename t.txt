 stage('List All Tools') {
            steps {
                script {
                    def allToolInstallations = ToolInstallation.all()

                    echo "Available tools:"
                    allToolInstallations.each { toolInstallation ->
                        echo "  - ${toolInstallation.name} (${toolInstallation.home})"
                    }
                }
            }
        }
